import { environment } from '../../environments/environment';

export const featureToggleEndpoint = `${environment.api_host}/feature-toggles`;
export const customerEndpoint = `${environment.api_host}/customers`;
